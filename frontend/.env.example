# Frontend Environment Variables
# Copy this to .env and fill in values

# Backend API URL (leave empty to use Vite proxy at /api)
# For local dev: leave blank (Vite proxies /api -> localhost:3001)
# For production: set to your deployed backend URL
VITE_API_URL=
